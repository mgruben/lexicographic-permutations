#include <iostream>
#include <vector>
using namespace std;

int factorial(int n) {
    if (n == 1 || n == 0) return 1;
    else return n * factorial(n - 1);
}

int indexPermutation(vector<int> perms, int index) {
    int ans = 0, r;
    do {
        
    } while (r != 0);
    return ans;
}

int main() {
    vector<int> perms {0, 1, 2};
    cout << factorial(10) << endl;
    return 0;
}
